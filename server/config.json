{
	"ROBOTS": [
		{
			"name": "robot-1",
			"port": 30003,
			"vnc": 5900,
			"address": "ursim",
			"camera": [
				{
					"name": "Static",
					"address": "rtsp://172.19.14.226:8554/cam"
				}
			]
		}
	],
	"DOCKER": {
		"OPTIONS": {
			"socketPath": "/var/run/docker.sock"
		},
		"MAX_VIRTUAL": 5,
		"NOTE": "To use the TCP proxy instead of the Unix socket, set DOCKER.OPTIONS to { \"host\": \"docker\", \"port\": 2375 } when the proxy runs in the same compose stack/network, or to { \"host\": \"localhost\", \"port\": 2375 } when running the server on the host while the proxy is mapped to 2375 on localhost."
	},
	"OAUTH": {
		"name": "SURFconext",
		"authorization_endpoint": "https://connect.test.surfconext.nl/oidc/authorize",
		"token_endpoint": "https://connect.test.surfconext.nl/oidc/token",
		"userinfo_endpoint": "https://connect.test.surfconext.nl/oidc/userinfo",
		"claims": {
			"id": "sub",
			"name": "name",
			"email": "email"
		}
	}
}
